import React from "react"
import "./layout.scss"
// import { Link } from "gatsby"

const Layout = ({ children }) => {
  return (
    <>
      <main>{children}</main>
    </>
  )
}

export default Layout
